/*
import React from 'react';
import style from './Button.module.css';

class Btn extends React.Component {
        render = () => {
                let increment = this.props.state.value === this.props.state.maxValue ? style.buttonMax : style.button;
                let reset = this.props.state.value === 0 ? style.buttonMax : style.button;

        return (
            <div className={style.butn}>
                    <button className={this.props.type} onClick={()=>{this.props.increment()}} disabled={this.props.state.value === 5}>Inc</button>

            </div>
        )}
}

export default Btn;*/
